import {useNewsList, useBreakingNews, useNewsGetByCategory} from './news';
import {setBookmark, useGetBookmark, deleteBookmark} from './bookmark';
import {useCategories} from './categories';
import {setUser, getUser} from './user';

export {
  useNewsList,
  setBookmark,
  useGetBookmark,
  useBreakingNews,
  useNewsGetByCategory,
  useCategories,
  deleteBookmark,
  setUser,
  getUser,
};
